import 'package:flutter/material.dart';

class TelegramBotItem extends StatelessWidget {
  const TelegramBotItem({Key key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return Container(
      child: Card(),
    );
  }
}
